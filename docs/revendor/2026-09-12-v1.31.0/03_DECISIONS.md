# 03 — Decisions

This run was non-interactive. The orchestrating session relayed the owner's standing answers from
2026-09-12: adopt any kit 17 surface that retires a local layer, but only where the suite stays
green; skip filing and pushing. So a decline is recorded here and handed back as a proposed issue.
None is filed.

| Candidate | Decision | Why |
|---|---|---|
| B1 `spec.bind` record-backed composer arm | **never** (proposed `state:will-not-do`, `severity:low`; unfiled) | AuraMaster addresses registry records through `container.`-relative schema paths, so the arm retires nothing here. Binding would take rows out of the schema and off `/am get|set`. It was built for PanelMaster#48 (LibKa0s v1.31.0, OptionsCompose minor 4) |
| B2 kit 17 harness surfaces | **nothing to adopt** | No local `tests/wow_mock.lua` layer has a kit 17 counterpart. The Ace shims were retired at v1.30.0 |

Not now: none. Unreached: none.
