# 05 - Summary: LibKa0s v1.55.0 -> v1.56.0

## The move

Tag `v1.55.0` -> `v1.56.0` (`514fc0a`), base taken from the `CLAUDE.md` provenance line and
confirmed against the last payload commit (`f6f3ffb`). Re-vendored in the RV-AM commit that carries
this bundle. Fifteen files moved a LibStub minor, none was added or removed, and no `NEEDS_*`
floor rose (the per-file table is `01_DELTA.md` 3b/3c). Test kit revision 25 -> 26.

No base correction: the v1.55.0 bundle states the right base (Step 0). No span bundle was written
here: the 3h listing is empty, because AM-02 (`d937092`) recorded v1.35.0 through v1.54.2.

## Delivered for free (class A)

- **Options minor 24**: `CreateOptionsPanel` parks in combat and replays at
  `PLAYER_REGEN_ENABLED`; this addon has no park of its own, so a `/reload` in combat now gets its
  panel after combat. `OpenOptionsPanel` answers a boolean (unused by the host).
- **OptionsTabs minor 4**: a banner or header page no longer leaks a widget and a texture per
  render. Every per-container page's `PageBanner` and the Containers page's `PageHeader` stop
  growing the frame count on each container switch.
- **OptionsWidgets minor 31**: the slider and color-picker drag throttles keep their own armed flag
  (no visible change here: the host's `scheduleTimer` already answers a handle).
- **Widgets minor 10**: `ReorderList` polls on the ghost frame and pools its line.
- **Slash minor 15**: `CliSet` / `CliReset` print a write-seam refusal (this addon's seam answers
  none and prints its own, so no visible change until AM-11).
- **Launcher minor 2**: the missing-library notice prints once, without the `[LibKa0s] ` prefix.
- **DebugLog minor 13**, **Perf minor 13**, **Media minor 4**, **Bus minor 2**, **Lifecycle
  minor 2**, **Core minor 8** (`printer.Format` survives a secret in a numeric slot): fixes that
  need no host change. **Item minor 2** and **Schema minor 2** arrive unconsumed.
- **Kit revision 26**: `asserts.lua` (`Kit.assertErrorMatches`, `Kit.assertLibraryConstant`),
  `mock_events.lua` (a recording `EventRegistry`, `__badEvents`), frames created shown, the AceDB
  fake raising and stripping defaults as AceDB-3.0 does, the lone-CR `test_eol` case, the prose gate
  reading the store-root files, section-sign case names, and the runner's `performance-§12` skip.

## Contract blockers

None (`01_DELTA.md` 3g). The five reds the library's dry run predicted for this addon (three
stand-down cases on frames created shown, one docs citation, one lone CR) were cleared by AM-01
(`6bb7027`, `63e7d55`) ahead of this commit.

## Adopted

Nothing in this run. Adoption decisions are this addon's M3 plan items: AM-07 (Core
`SafeRegisterEvent`), AM-09 (Launcher `isEnabled` / `disabledLine`), AM-11 (the Slash seam's
answers), AM-15 (Schema, AuraMaster#21), AM-16 (`writeThrough` and the degraded stubs), and AM-17
(`RenderTabbedSchema` `opts.tabs` / `disabledFor` / `chrome`, `PageBanner` `action`).

## Declined

None in this run.

## Skipped or unreached

- Steps 5-8 (candidates, interview, adoption, issue filing): by the plan, owned by the M3 items
  above.
- `docs/test-cases.md` and the README test badge: regenerated by plan item AM-DOCS, never by the
  re-vendor commit.

## Suite results

Every run from the repo root through `ka0s-bounded`. Tests are passed / failed / skipped; lint is
warnings / errors.

| Gate | Headless tests | Lint | Complexity |
|---|---|---|---|
| Before the copy (v1.55.0, kit 25) | 1296 / 0 / 0 | 0 / 0 in 110 files | not run |
| After the copy (v1.56.0, kit 26) | 1296 / 0 / 0 | 0 / 0 in 110 files | 2 above CCN 15, both pre-existing (AM-13) |
