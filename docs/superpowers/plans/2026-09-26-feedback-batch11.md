# Feedback batch 11 Implementation Plan

> Run as Workflow orchestration (ultracode): one implementer and one independent reviewer per task.

**Spec:** `docs/superpowers/specs/2026-09-26-feedback-batch11-design.md` (binding). The batch 9 plan's
global constraints and resume rules apply unchanged, with commit prefix `B11-`.

**Lesson (batch 10 F7):** a migration step already pushed to the branch is never extended again. Add a new step.

## Status ledger

| Task | Req | Status | Commit | Notes |
|---|---|---|---|---|
| T1 points model, defaults, v11 migration | G2 G3 G4 G5 | done | the `B11-T1: ` commit (a commit cannot hold its own hash) | KEY NAMES: the child point is stored as `attach.childPoint`, not the spec's `attach.point`, because `attach.point`/`.relativePoint` already hold a named frame's corners with template defaults (TOPLEFT/BOTTOMLEFT) that the backfill stamps on every container, so nil could never mean Automatic there; the parent point is `attach.relPoint` as specified; neither is declared in the template (nil = Automatic; T2's rows must keep that, e.g. no template default or a sentinel the loader keeps: `normalizeAttach` drops any stored value that is not one of the nine WoW points). Anchors: `AttachPoints(cfg)` (point, rel, pointAuto, relAuto, growH, growV), `DefaultEdge(cfg)` is G3 (Text child by its justify, icons/bars under a CENTER Text parent centered, else after-start), `AttachEdge(cfg)` classifies (token or nil = free) via tables built at load (allocation-free), `EdgePoints` reads the same tables; attachSpec/ownSide/JoinText/pin use them, a free pair is placed at X/Y alone with no seam, spread or push and its strip/label on its own before side (H0); `EdgeAllowed` refuses no token, `ResolvedEdge`/`EdgeLabel`/`C.EDGE_SIDE_LABELS` removed; FLOW_PATHS: childPoint, relPoint, style, text.justifyH (perLine and attach.edge out); ContainerManager PARENT_PATHS: the two points. Layout: batch 9's Side row, its values/validate, the fallback note and the new-attachment stamp removed (an attachment writes no points; picks survive attach, retarget and detach), the joins line reads AttachPoints; 12 dead locale keys removed, the Container row help reworded. Database: template `edge` removed; `MigrateV11` + ladder row {to=11}: drop after-start (and an unknown token), convert any other side to the points batch 10 resolved it to (behind on a wide child read as after, the chain root's growth from that profile's own raw containers, frozen in the step), keep stored points, remove `attach.edge`, idempotent; normalizeAttach now validates the points. Tests: new suite test_anchors_points (17); v11 tests in test_migrations (unit, every growth vs EdgePoints, kept points, ladders from v10/v9/v8/v1 in every profile, load normalize); edges/strip/column/pages/diagnostics/database cases rewritten for points; schema 10s -> 11 (per-profile log count 20). Docs: schema.md (attach rows, load pass, v11 entry, 256 rows, Layout 36), ARCHITECTURE, data-flow, known-limitations, module-map, settings-panel, README, citations remapped; test-cases + badge 1604 |
| T2 two anchor-point dropdowns, `/am set`, diagnostics | G1 G7 | done | the `B11-T2: ` commit | Layout > Anchor > Another container: 'Parent container anchor point' (`container.attach.relPoint`) and 'This container anchor point' (`container.attach.childPoint`, T1's key, because `container.attach.point` is already the Named frame Point row), each 'Automatic (<point in effect>)' then the nine points, any pair stored, no validate refusal, onChange structural (the joins line and Automatic labels redraw; CONFIG_CHANGED re-places the container, parent and followers). Storage stays nil for Automatic through a new row field `nilAs = "auto"` in settings/Schema.lua (GetSetting and DefaultFor read nil as auto, the row default is auto so Defaults and /am reset work, the Validate defaultsRoot answers it, normalize writes auto back as nil); `/am set` folds case through a row `cliParse` hook on the Slash descriptor's `parse`; `container.attach.edge` is no path. Diagnostics [Cont] prints `attach=container#N point=X(auto|picked) relPoint=Y(auto|picked) join=<token|free>`. JoinText's tooltip now says 'Change the anchor points'. Tests: 4 layout + 1 diag (1609). Docs: schema.md, settings-panel.md, slash-dispatch.md, debug.md, ARCHITECTURE, test-cases, README badge, citations remapped. Review (B11-T2R): the Automatic entry named the pick while a row held one ("Automatic (Top)" with Top picked); it now names Automatic's own point through a new `Anchors.AutoPoints` (1 layout test, 1610); data-flow.md's stale `Anchors.Place` citation fixed |
| T3 remove the join dot | G6 | done | the `B11-T3: ` commit | Anchors' `buildPin`/`updatePin` (and `PIN_SIZE`/`PIN_SIDE`, the UpdateHandle call), Container's Park/Destroy `joinPin` hides and `C.JOIN_PIN_COLOR` deleted; no locale strings existed. The two strip pin tests and the column pin test replaced by one strip test that no frame turns a texture into a diamond and no `joinPin` exists, unlocked or in test mode, the strip still shown (1608). Test-mode outline and the tooltip's Joined-to line kept. Docs: known-limitations (nothing on screen marks a join), data-flow, module-map, README, smoke checks 222 and 226-228 and their tables, test-cases regenerated, badge, schema.md's Constants citation remapped |
| T4 docs sync, smoke section AC, full battery | all | done | the `B11-T4: ` commit | smoke-tests.md section AC, checks 236-245 with its index table (none marked passed; the AC intro and the AA/AB rows note which Side and attach.edge steps batch 11 retires); debug.md's example header schema v11; settings-panel.md's facing-growth sentence no longer claims a follower's points never face back. Verified: live schema 258 rows (General 18, Containers 5, Filters 46, Layout 38, Bars 72, Icons 42, Text 37), SCHEMA_VERSION 11; test-cases.md regenerated byte-identical, badge 1608. Battery: 1608 passed 0 failed, luacheck 0/0, lizard 0 warnings, largest file 1480 lines, tests/perf.lua bytes/iter identical to B10-C in every scenario. Review (B11-T4R): check 242 and schema.md's v11 entry said only a Text-under-Text follower could move on migration; a dropped after-start also moves a Text follower justified Center or to its end side under any parent, and an Icons or Bars follower under a centered Text parent. Both now say so, and known-limitations records it |
| T5 the attach-target row reads "Parent container"; AB owner result | owner 2026-09-26 | done | the `B11-T5: ` commit | Layout > Anchor > Another container's Container row relabeled "Parent container" (new NS.L key; the Containers banner picker keeps "Container"); test_pages_layout finds the row by the new label; smoke 236, settings-panel, schema updated; the owner's AB run recorded in smoke-tests.md |
| T6 no jump on a target switch | owner 2026-09-26 | done | the `B11-T6: ` commit | owner: unlocked, switching targets, a child snapped left and back within a second. The engine redraws for the new unit in the frame of PLAYER_TARGET_CHANGED while the empty prediction ran 0.2 s later, so a follower hung from an emptied parent sat on the engine's 1x1 rect for the delay; EmptyWatch now runs the pass at once on a target or focus switch, canceling one already due; two tests, ARCHITECTURE/data-flow/known-limitations/performance, test-cases + badge 1610 |
| T7 dim-gold strip name when attached; Named frame anchor-point rows | owner 2026-09-26 | done | the `B11-T7: ` commit | the strip name reads C.SECONDARY_GOLD while attached to a container or a named frame (handleText, the TEST tag unchanged); Named frame's rows relabeled Named frame anchor point (relativePoint, left) and This container anchor point (point, right), swapped; two tests; settings-panel, data-flow, smoke section AD (246-247), test-cases + badge 1612 |
| T8 gray, not dim gold; the tooltip says it cannot be dragged | owner 2026-09-26 | done | the `B11-T8: ` commit | the owner found T7's dim gold not muted enough: new C.ATTACHED_NAME_COLOR ff8c8a84 (warm gray); the strip and "?" tooltip's first line reads "Anchored to '<parent container or frame>', so it cannot be dragged. Right-click for settings." while attached, "Drag to move" otherwise; tests updated red-first; data-flow, smoke 246, schema citation |
| T9 a center or end join holds steady on an empty parent | owner 2026-09-26 | done | the `B11-T9: ` commit | owner: bars chain growing up joined Bottom to Top, Target Debuffs shifted left half a bar (132 of 262 px) whenever Target Buffs (Mine) was empty, locked, unlocked and in combat: an empty engine's 1x1 rect sits at its start corner. Anchors attachSpec now moves the relative point to the parent's start side on each axis the parent is one element across (perLine 0 on its fill axis, or lines of one) and the join does not run along, adding half or all of the parent's one-element size (Style.ElementSize, its Scale converted to the child's); classification, seam, spread and push read the unmoved points; the along-join axis is left alone so an empty parent still closes the chain up and every start-aligned pair is unchanged. New suite test_anchors_steady (7); four center/end expectations updated to the steadied point; data-flow, known-limitations (a parent several across), module-map, smoke 248. Review (B11-T9R): test-cases.md regenerated after the new suite was tracked (its census read 173 authored files, not 174) |

## Checkpoint log

| When | Milestone | Evidence |
|---|---|---|
| 2026-09-26 | B11 | 1608 passed / 0 failed / 0 skipped (16 shards); luacheck 0 warnings / 0 errors in 136 files; lizard no function above CCN 15 (4233 functions, 0 warnings); head 6c0bc41 |
